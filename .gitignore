node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# api/[...slug].js IS committed on purpose (see its header): it is the single
# /api function Vercel detects from the source tree, regenerated by
# `npm run build`. Ignore only the old build-artifact name and stray local files.
api/*.mjs
